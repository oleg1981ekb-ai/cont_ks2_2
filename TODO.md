# TODO — feature/dynamic-extra-documents

## Шаги
- [ ] 0) Убедиться, что ветка `feature/dynamic-extra-documents` активна.

- [x] 1) `config.py`: добавить в `DOCUMENTS_LIST` новые типы документов.

- [x] 2) `config.py`: добавить `DOCUMENT_ROLES` (маски доступности подписантов) для новых типов:

  - «Акт передачи оборудования»: активна только СтрК, СДО и ГенДир блокируются
  - «Акт расхода давальческих материалов»: активны СтрК, СДО, ГенДир

- [x] 3) `database.json`-схема: определить и реализовать хранение опциональности на уровне месяца.

  - Предложение реализации: в `db[dir][sub][month]` добавить поле `extra_docs` (list) с типами документов, которые добавлены пользователем.

- [x] 4) `wizard_edits.py`: добавить пункт меню для выбранного месяца:

  - список новых типов документов
  - возможность добавить документ в `extra_docs` или удалить из `extra_docs`

- [x] 5) `builder.py`: изменить вывод строк документов.


   - Для стандартных документов (КС-2/КС-3/...) — выводить всегда как раньше.
   - Для новых документов — выводить только если они присутствуют в `extra_docs` конкретного месяца.

- [x] 6) `builder.py`: проверить автоподстановку GenДир для КС-2/КС-3 не сломалась.



- [x] 7) Прогнать локальную генерацию Excel (`python3 main.py`) и убедиться, что:

  - добавленные пользователем новые документы печатаются в нужном месяце
  - удалённые — исчезают

- [ ] 8) Зафиксировать изменения в git (commit/push через wizard_git.run_git_sync()).

