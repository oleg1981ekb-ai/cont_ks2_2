# TODO

## 1) Понять, где в коде выбирается/меняется статус 1/2/3
- [x] Открыть `wizard_edits.py` и найти ветку `sub_choice == "3"` (изменение статусов)

## 2) Спроектировать вывод текущих значений статусов при выборе нового (КОД НЕ МЕНЯТЬ БЕЗ СОГЛАСИЯ)
- [ ] Добавить вывод текущего `value`/`date` перед вводом `st_choice`.
  - [ ] В режиме apply_mode=="1" выводить текущее значение по `status_key` для периода (если статус в базе хранится в старом/новом формате).
  - [ ] В режиме apply_mode=="2" выводить текущее значение для выбранного документа и `status_key` перед вводом `st_choice`.
- [ ] Убедиться, что вывод корректно работает для обоих форматов `database.json`:
  - [ ] `status = {"value": ..., "date": ...}`
  - [ ] `status[doc_name][status_key] = {"value": ..., "date": ...}`

## 3) Реализовать правки после согласования пользователя
- [ ] Внести минимальные изменения только в консольный вывод.
- [ ] Не менять алгоритм присвоения статусов.

## 4) Проверка
- [ ] Запустить программу и проверить сценарии:
  - [ ] apply_mode=1
  - [ ] apply_mode=2

